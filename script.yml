---
- >-
  dcb
  --snippetsdir .
  --snippet from.j2 entrypoint.j2 ansible-playbook.j2
  --upstreamgroup andrewrothstein
  --upstreamapp docker-ansible
  --pull ${OS}
  --write ${OS}
  --build ${OS}
  --push ${OS}
